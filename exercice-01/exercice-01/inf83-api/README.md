# Changelog

## [1.0.0] - 2026-06-23

### Added
- Route GET /health : vérifie que le serveur tourne
- Route GET /ready : vérifie la connexion PostgreSQL
- Route GET /items : retourne la liste des items
- Route GET /version : retourne la version de l'API

### Changed
- (aucun changement pour cette première version)

### Fixed
- (aucun correctif pour cette première version)